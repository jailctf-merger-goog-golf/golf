# golf

## running web server

run `python3 main.py` in the directory containing this README.md

## building new js stuff

run `npm i` to install packages

modify `script.js` as you wish

run `node build.js` to build the `bundle.js` file

done
